import { tw } from '@/components/tw'

export const labelClasses = {
  base: tw(``),
  title: tw(`text-[50px] md:text-[90px] text-title`),
  'sub-title': tw(`text-[28px] md:text-[36px] text-sub-title`),
  light: tw(``),
  dark: tw(`text-white`),
}
